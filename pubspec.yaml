name: true_love_app
description: تطبيق الحب الحقيقي للمطلقين
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter: { sdk: flutter }
  flutter_localizations: { sdk: flutter }
  # هنا التعديل النهائي: استخدم backendless_flutter_sdk
  backendless_flutter_sdk: ^7.3.0
  flutter_riverpod: ^2.5.1
  flutter_card_swiper: ^7.2.0
  google_mobile_ads: ^7.0.0
  cached_network_image: ^3.4.1
  image_picker: ^1.2.1
  shared_preferences: ^2.5.4
  intl: ^0.18.1

flutter:
  uses-material-design: true
